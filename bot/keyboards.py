import json

class KeyboardBuilder:
    """ Метод создания клавиатуры. """
    @staticmethod
    def main_keyboard():
        keyboard = {
            # кнопки клавиатуры, после осмысления логики и функционала бота
        }
        return json.dumps(keyboard, ensure_ascii=False)